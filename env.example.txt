# 复制成 .env 后按需修改，docker compose 会自动读取
# 全部有默认值，不改也能直接跑

DB_PASSWORD=planner123
DB_NAME=student_planner

# 模型。换成 qwen3:8b / qwen3:14b 只需改这一行，重启后会自动下载
OLLAMA_MODEL=qwen3:4b

# 本地模型生成慢，单次请求超时（秒）
OLLAMA_TIMEOUT=180

# 前端访问端口，被占用的话改这里
WEB_PORT=8080
